(function(w) {
  'use strict';

  var Settings = {
    startHeight: 300, // pixel
    gravity: 300, // pixel/s
    interval: 20, // ms
    jumpingPower: 200, // pixel/s
    groundHeight: 600
  };

  w.Settings = w.Settings || Settings;
})(window);
